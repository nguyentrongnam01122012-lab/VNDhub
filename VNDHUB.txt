-- ✅ BẢN CHUẨN ĐỒNG BỘ VND💵 + FARM BÁNH MÌ + FARM BỆNH VIỆN (TAB 3)
repeat task.wait() until game:IsLoaded()

-- 🟣 Tải giao diện Fluent bằng link gốc chính thức
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- 🪟 Cửa sổ chính
local Window = Fluent:CreateWindow({
	Title = "VND💵",
	SubTitle = "Version 1.0",
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Dark",
	MinimizeKey = Enum.KeyCode.End
})

-- 🟢 Nút mở/tắt menu chính
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "http://www.roblox.com/asset/?id=126677255840605"

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
    Window:Minimize()
end)

-- 📑 Các tab chính
local Tabs = {
	Main1 = Window:AddTab({ Title = "Main 🏠" }),
	Main2 = Window:AddTab({ Title = "Farm Level ⚔️" }),
	Main3 = Window:AddTab({ Title = "Farm Tiền 💵" }),
	Main4 = Window:AddTab({ Title = "Farm Boss 👹" }),
	Main5 = Window:AddTab({ Title = "PVP Combat 🥊" }),
	Main6 = Window:AddTab({ Title = "Fix Lag ⚡" }),
	Main7 = Window:AddTab({ Title = "Settings ⚙️" })
}

------------------------------------------------
-- CẤU HÌNH LOGIC CHUNG & CÀY CẤP QUÁI THƯỜNG
------------------------------------------------
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local player = Players.LocalPlayer

_G.AutoFarmLevel = false 

local function isAliveModel(model)
    if model and model:FindFirstChild("Humanoid") and model.Humanoid.Health > 0 then
        return true
    end
    return false
end

local function getNearestMonster()
    local char = player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local hrpPos = char.HumanoidRootPart.Position
    local closestMonster = nil
    local maxDistance = math.huge
    local targetFolder = workspace:FindFirstChild("Bosses") or workspace:FindFirstChild("Monsters") or workspace
    
    for _, obj in ipairs(targetFolder:GetChildren()) do
        if obj:IsA("Model") and isAliveModel(obj) and obj ~= char then
            if not Players:GetPlayerFromCharacter(obj) then 
                local mHrp = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso")
                if mHrp then
                    local distance = (hrpPos - mHrp.Position).Magnitude
                    if distance < maxDistance then
                        maxDistance = distance
                        closestMonster = obj
                    end
                end
            end
        end
    end
    return closestMonster
end

task.spawn(function()
    while true do
        task.wait()
        if _G.AutoFarmLevel then
            pcall(function()
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local target = getNearestMonster()
                
                if hrp and target and isAliveModel(target) then
                    local targetHrp = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChild("Torso")
                    if targetHrp then
                        hrp.CFrame = CFrame.new(targetHrp.Position + Vector3.new(0, 3, 0), targetHrp.Position)
                        VirtualUser:ClickButton1(Vector2.new())
                    end
                end
            end)
        end
    end
end)

------------------------------------------------
-- LOGIC TREO CHÙA, BÁNH MÌ, BỆNH VIỆN & ANTI-AFK
------------------------------------------------
_G.AutoFarmChua = false
_G.AutoFarmBanhMi = false 
_G.AutoFarmBenhVien = false -- Biến điều khiển Farm Bệnh Viện mới
_G.AntiAFKEnabled = false 

local ToaDoChua1 = Vector3.new(1561, 133, -244)
local ToaDoChua2 = Vector3.new(1547, 133, -245)

-- Tọa độ ghế bánh mì
local TDBanhMi = {
    Vector3.new(573, 21, -357),
    Vector3.new(563, 20, -386),
    Vector3.new(553, 20, -413),
    Vector3.new(531, 21, -449)
}

-- Tọa độ ghế bệnh viện của bạn
local TDBenhVien = {
    Vector3.new(790, 22, -403),
    Vector3.new(790, 22, -413)
}

-- Hàm kiểm tra ghế trống
local function isSeatOccupied(targetPos)
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("Seat") or v:IsA("VehicleSeat") then
            if (v.Position - targetPos).Magnitude <= 8 then
                if v.Occupant then return true end
            end
        end
    end
    return false
end

-- Hàm kích hoạt ProximityPrompt để ngồi vào ghế
local function interactWithSeatPrompt(targetPos)
    for _, prompt in ipairs(workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") and (prompt.Parent:IsA("BasePart") or prompt.Parent:IsA("Model")) then
            if (prompt.Parent.Position - targetPos).Magnitude <= 6 then
                fireproximityprompt(prompt)
                break
            end
        end
    end
end

-- Hàm tối ưu đồ họa (FPS Boost)
local function activateFPSBoost()
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    game:GetService("Lighting").GlobalShadows = false
    game:GetService("Lighting").Brightness = 0
    game:GetService("Lighting").FogEnd = 1e9
    for _,v in ipairs(workspace:GetDescendants()) do
        if v:IsA("Decal") or v:IsA("Texture") or v:IsA("Sound") then pcall(function() v:Destroy() end) end
        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then v.Enabled = false end
        if v:IsA("BasePart") then
            v.Material = Enum.Material.SmoothPlastic
            v.Reflectance = 0
            v.CastShadow = false
        end
    end
end

-- Hàm khôi phục đồ họa
local function deactivateFPSBoost()
    game:GetService("Lighting").GlobalShadows = true
    game:GetService("Lighting").Brightness = 2
    game:GetService("Lighting").FogEnd = 1000
end

-- Hệ thống chặn Roblox Kick AFK (Tự kích hoạt khi bật bất kỳ tính năng farm treo máy nào)
player.Idled:Connect(function()
    if _G.AntiAFKEnabled or _G.AutoFarmBanhMi or _G.AutoFarmChua or _G.AutoFarmBenhVien then
        VirtualUser:Button2Down(Vector2.new(), workspace.CurrentCamera.CFrame)
        task.wait(0.5)
        VirtualUser:Button2Up(Vector2.new(), workspace.CurrentCamera.CFrame)
    end
end)

-- Vòng lặp di chuyển camera chống afk định kỳ
task.spawn(function()
    while true do
        task.wait(250)
        if _G.AntiAFKEnabled or _G.AutoFarmBanhMi or _G.AutoFarmChua or _G.AutoFarmBenhVien then
            pcall(function()
                workspace.CurrentCamera.CFrame *= CFrame.Angles(0, math.rad(5), 0)
                VirtualUser:Button2Down(Vector2.new(), workspace.CurrentCamera.CFrame)
                task.wait(0.5)
                VirtualUser:Button2Up(Vector2.new(), workspace.CurrentCamera.CFrame)
            end)
        end
    end
end)

-- Vòng lặp 1: Xử lý di chuyển Treo Chùa
task.spawn(function()
    while true do
        task.wait(0.3)
        if _G.AutoFarmChua then
            pcall(function()
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local humanoid = char and char:FindFirstChildOfClass("Humanoid")
                
                if hrp and humanoid and not humanoid.SeatPart then
                    local finalTarget = ToaDoChua1
                    if isSeatOccupied(ToaDoChua1) then finalTarget = ToaDoChua2 end
                    
                    hrp.CFrame = CFrame.new(finalTarget + Vector3.new(0, 2, 0))
                    interactWithSeatPrompt(finalTarget)
                end
            end)
        end
    end
end)

-- Vòng lặp 2: Xử lý di chuyển Farm Bánh Mì (Tự quét ghế trống 1-4)
task.spawn(function()
    while true do
        task.wait(0.3)
        if _G.AutoFarmBanhMi then
            pcall(function()
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local humanoid = char and char:FindFirstChildOfClass("Humanoid")
                
                if hrp and humanoid and not humanoid.SeatPart then
                    local finalTarget = nil
                    
                    for i = 1, #TDBanhMi do
                        if not isSeatOccupied(TDBanhMi[i]) then
                            finalTarget = TDBanhMi[i]
                            break 
                        end
                    end
                    
                    if not finalTarget then finalTarget = TDBanhMi[1] end
                    
                    hrp.CFrame = CFrame.new(finalTarget + Vector3.new(0, 2, 0))
                    interactWithSeatPrompt(finalTarget)
                end
            end)
        end
    end
end)

-- Vòng lặp 3: Xử lý di chuyển Farm Bệnh Viện (Tự quét ghế trống 1-2)
task.spawn(function()
    while true do
        task.wait(0.3)
        if _G.AutoFarmBenhVien then
            pcall(function()
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local humanoid = char and char:FindFirstChildOfClass("Humanoid")
                
                if hrp and humanoid and not humanoid.SeatPart then
                    local finalTarget = nil
                    
                    for i = 1, #TDBenhVien do
                        if not isSeatOccupied(TDBenhVien[i]) then
                            finalTarget = TDBenhVien[i]
                            break 
                        end
                    end
                    
                    if not finalTarget then finalTarget = TDBenhVien[1] end
                    
                    hrp.CFrame = CFrame.new(finalTarget + Vector3.new(0, 2, 0))
                    interactWithSeatPrompt(finalTarget)
                end
            end)
        end
    end
end)

------------------------------------------------
-- TAB 1: Main 🏠
------------------------------------------------
Tabs.Main1:AddButton({
	Title = "tiktok bạn:)",              
	Description = "oách xà lách vô cùng",
	Callback = function()
		setclipboard("https://www.tiktok.com/@jannie3342?_t=ZS-90QsJQ5j0NO&_r=1")  
	end
})

------------------------------------------------
-- TAB 2: Farm Level ⚔️
------------------------------------------------
Tabs.Main2:AddToggle("AutoFarmLevelToggle", {
    Title = "Tự Động Cày Cấp (Auto Farm)", 
    Default = false, 
    Callback = function(Value)
        _G.AutoFarmLevel = Value
    end
})

Tabs.Main2:AddToggle("AutoFarmChuaToggle", {
    Title = "Auto Farm Ở Chùa", 
    Default = false, 
    Callback = function(Value)
        _G.AutoFarmChua = Value
    end
})

Tabs.Main2:AddToggle("AntiAFKToggle", {
    Title = "Kích Hoạt Anti-AFK", 
    Default = false, 
    Callback = function(Value)
        _G.AntiAFKEnabled = Value
    end
})

Tabs.Main2:AddToggle("FPSBoostToggle", {
    Title = "Kích Hoạt FPS Boost", 
    Default = false, 
    Callback = function(Value)
        if Value then
            activateFPSBoost()
        else
            deactivateFPSBoost()
        end
    end
})

Tabs.Main2:AddButton({
    Title = "admin",                             
	Description = "script này ngon á đủ chức năng cả😃",
	Callback = function()
		loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Infinite-Yield-43437"))() 
	 end
})

Tabs.Main2:AddButton({
    Title = "admin",
    Description = "đang fix farm npc😃",
	Callback = function()
        loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-V-fly-20577"))()
     end
})

------------------------------------------------
-- TAB 3: Farm Tiền 💵
------------------------------------------------
-- Công tắc 1
Tabs.Main3:AddToggle("AutoFarmBanhMiToggle", {
    Title = "Auto Farm Bánh Mì", 
    Default = false, 
    Callback = function(Value)
        _G.AutoFarmBanhMi = Value
    end
})

-- Công tắc 2 (Nằm dưới Farm Bánh Mì + Tích hợp sẵn Anti-AFK ngầm)
Tabs.Main3:AddToggle("AutoFarmBenhVienToggle", {
    Title = "Auto Farm Bệnh Viện", 
    Default = false, 
    Callback = function(Value)
        _G.AutoFarmBenhVien = Value
    end
})

-- Thông báo hệ thống VND💵
Fluent:Notify({
    Title = "VND💵 System",
    Content = "Menu đã sẵn sàng kích hoạt! Chúc bạn may mắn.",
    Duration = 4
})
